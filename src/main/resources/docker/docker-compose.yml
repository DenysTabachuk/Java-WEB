services:
  postgres:
    image: postgres:latest
    container_name: space_cats_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: space_cats_marketplace
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres